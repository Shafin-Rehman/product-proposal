import ResetPasswordForm from '@/components/reset-password-form'

export const metadata = {
  title: 'Reset Password — BudgetBuddy',
}

export default function ResetPasswordPage() {
  return <ResetPasswordForm />
}
